// Create a simple calculator program in JavaScript which can perform the addition, substraction, multiplication and division on given numbers.
// User Input
let n1 = 12;
let n2 = 2;
let operator = "/";

    if (operator == "+") {
        output = n1 + n2;
        console.log(`The sum of the above two digits are ${output}`)
    }
    
    else if (operator=="-"){
        output = n1-n2;
        console.log(`The subtraction of the above two digits are ${output}`)
    }
    else if (operator=="*"){
        output = n1*n2;
        console.log(`The multiplication of the above two digits are ${output}`)
    }
    else if (operator=="/"){
        output = n1/n2;
        console.log(`The division of the above two digits are ${output}`)
    }






